# 🔄 הוראות עדכון - Timer 24H Integration

## מה חדש בגרסה זו?

✅ **רישום אוטומטי של Lovelace Resource**  
✅ **Cache Busting אוטומטי** - אין יותר בעיות עם דפדפן שלא מראה עדכונים  
✅ **עדכון אוטומטי של גרסאות** - בכל עדכון ה-URL מתעדכן אוטומטית

---

## 📋 הוראות עדכון

### שלב 1: עדכן את האינטגרציה

**דרך HACS**:
1. פתח HACS
2. חפש "Timer 24H Integration"
3. לחץ על "Update"
4. המתן לסיום ההתקנה

**דרך ידנית**:
1. הורד את הגרסה החדשה מ-GitHub
2. החלף את התיקייה `custom_components/timer_24h`
3. המשך לשלב הבא

### שלב 2: נקה Resources ישנים (חשוב!)

1. עבור ל: **Settings → Dashboards → Resources**
2. חפש את `timer-24h-card.js`
3. **אם ה-URL לא כולל `?v=`** - **מחק** את ה-resource הזה
4. דוגמה למחיקה:
   - ❌ `/local/timer-24h-card/timer-24h-card.js` (בלי version)
   - ✅ `/local/timer-24h-card/timer-24h-card.js?v=4.6.7` (עם version - אל תמחק!)

### שלב 3: אתחל את Home Assistant

1. עבור ל: **Settings → System → Restart**
2. לחץ על **"Restart Home Assistant"**
3. המתן לאתחול

### שלב 4: רענן את הדפדפן

**חשוב מאוד!** בצע **Hard Refresh**:

- **Windows/Linux**: `Ctrl + Shift + R` או `Ctrl + F5`
- **Mac**: `Cmd + Shift + R`
- **Chrome**: `Ctrl + Shift + Delete` → נקה Cache

### שלב 5: בדיקה

1. עבור ל-Dashboard שלך
2. בדוק שהכרטיס מוצג
3. ודא שהוא פועל כראוי

---

## ✅ איך לבדוק שהכל עובד?

### בדוק את ה-Resource:

1. עבור ל: **Settings → Dashboards → Resources**
2. אתה אמור לראות:
   ```
   /local/timer-24h-card/timer-24h-card.js?v=4.6.7
   ```
3. ודא שיש `?v=` בסוף ה-URL!

### בדוק את הלוגים:

1. עבור ל: **Settings → System → Logs**
2. חפש הודעות:
   - ✅ `Timer 24H Card v4.6.7 installed successfully`
   - ✅ `Registered Timer 24H Card resource version 4.6.7`
   - ✅ `Updated Timer 24H Card resource to version 4.6.7`

---

## 🎉 זהו! מעכשיו:

✨ **עדכונים עתידיים** יעבדו אוטומטית  
✨ **אין צורך** להוסיף resources ידנית  
✨ **אין בעיות cache** - הדפדפן תמיד יטען את הגירסה החדשה  

---

## ❓ בעיות?

### הכרטיס לא מופיע

1. בדוק שה-resource נרשם (ראה למעלה)
2. נקה cache של הדפדפן (`Ctrl + Shift + R`)
3. אתחל שוב את Home Assistant
4. בדוק logs לאיתור שגיאות

### ה-Resource לא מתעדכן

1. מחק את ה-resource הישן **ידנית** (ללא `?v=`)
2. אתחל את Home Assistant
3. בדוק שה-resource החדש נוצר עם `?v=`

### עדיין רואה גירסה ישנה

1. **נקה לגמרי את ה-cache**:
   - Chrome: `Ctrl + Shift + Delete`
   - בחר "All time"
   - בחר "Cached images and files"
   - לחץ "Clear data"
2. סגור את הדפדפן **לגמרי**
3. פתח מחדש ועבור ל-Home Assistant
4. בצע `Ctrl + Shift + R`

### מצב אינקוגניטו עובד אבל רגיל לא

זו בעיית cache קלאסית! עשה:
1. `Ctrl + Shift + Delete`
2. נקה **הכל**
3. אתחל את הדפדפן
4. בדוק שוב

---

## 📞 צריך עזרה?

- 🐛 **דווח על באג**: [GitHub Issues](https://github.com/davidss20/home-assistant-24h-timer-integration/issues)
- 💬 **דיון**: [GitHub Discussions](https://github.com/davidss20/home-assistant-24h-timer-integration/discussions)

---

**עדכון זה פותר את בעיית ה-cache אחת ולתמיד! 🎊**

